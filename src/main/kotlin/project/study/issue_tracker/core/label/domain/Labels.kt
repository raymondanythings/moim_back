package project.study.issue_tracker.core.label.domain

class Labels {

    companion object {
        fun createBaseLabels(issueId: Long): List<Label> {
            return listOf()
//            return listOf(
//                createGreenLabel(issueId)
//            )
        }
    }
}
